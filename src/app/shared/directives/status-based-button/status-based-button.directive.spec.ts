import { StatusBasedButtonDirective } from './status-based-button.directive';

describe('StatusBasedButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBasedButtonDirective();
    expect(directive).toBeTruthy();
  });
});
