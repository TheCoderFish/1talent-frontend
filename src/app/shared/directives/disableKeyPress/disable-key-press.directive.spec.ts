import { DisableKeyPressDirective } from './disable-key-press.directive';

describe('DisableKeyPressDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableKeyPressDirective();
    expect(directive).toBeTruthy();
  });
});
