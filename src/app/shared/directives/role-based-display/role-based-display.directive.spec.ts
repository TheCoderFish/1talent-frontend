import { RoleBasedDisplayDirective } from './role-based-display.directive';

describe('RoleBasedDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleBasedDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
