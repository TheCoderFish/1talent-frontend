import { StatusBasedDisplayDirective } from './status-based-display.directive';

describe('StatusBasedDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBasedDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
